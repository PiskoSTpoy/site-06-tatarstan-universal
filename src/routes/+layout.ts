export const prerender = true;
export const trailingSlash = 'always';

/*
 * Клиентский рендер выключен по умолчанию для всего сайта и включён точечно там, где без него
 * не работает функциональность (src/routes/+page.ts — калькулятор допуска и форма заявки).
 *
 * Почему так. Остальные 37 страниц — статический текст: чек-листы, разборы норм, реестр парка.
 * Гидрация не даёт им ничего, но стоит каждому посетителю 103–133 КБ JS: SvelteKit кладёт в
 * чанк страницы копию всей её разметки, чтобы построить то же самое ещё раз поверх уже
 * пришедшего HTML. Декоративные механизмы (scroll-reveal, оглавление, полоса прогресса,
 * count-up) вынесены в static/enh.js — 4 КБ на весь сайт.
 */
export const csr = false;
