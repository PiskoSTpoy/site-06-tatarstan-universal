/*
 * Единственная страница сайта с клиентским рендером: здесь живут калькулятор допуска
 * (DopuskWizard) и форма заявки — без JS они не работают, а не просто теряют украшение.
 * Настройка страницы перекрывает `csr = false` из +layout.ts.
 */
export const csr = true;
